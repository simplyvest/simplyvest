# Google Sheets API (for waitlist)
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_PRIVATE_KEY=
GOOGLE_SHEET_ID=
GOOGLE_SHEET_NAME=Sheet1

# Solana RPC (optional, defaults to devnet)
SOLANA_RPC_URL=

# Privy (auth)
PRIVY_APP_ID=
PRIVY_APP_SECRET=

# D1 Database (configured via wrangler.toml, not .env)
# DB=bound via wrangler.toml
