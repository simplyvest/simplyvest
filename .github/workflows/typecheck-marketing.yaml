name: Typecheck Marketing

on:
  workflow_call:

jobs:
  typecheck-marketing:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/pnpm-setup
      - name: Typecheck marketing
        run: pnpm --filter @simplyvest/marketing typecheck
