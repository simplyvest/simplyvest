name: Typecheck Web

on:
  workflow_call:

jobs:
  typecheck-web:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/pnpm-setup
      - name: Build SDK
        run: pnpm --filter @solana-tdp/sdk build
      - name: TypeScript check (web)
        run: pnpm --filter @solana-tdp/web check:ts
